{
  "rules": {
    "presence": {
      "$quizId": {
        "$uid": {
          ".read": false,
          ".write": "auth != null && auth.uid == $uid",
          ".onDisconnect": true
        }
      }
    }
  }
}
